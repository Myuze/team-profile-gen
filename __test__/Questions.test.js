const Questions = require('../src/Questions');

describe('Questions', () => {
  describe('functions', () => {
    // It Prompts Questions needed for application

    // It Sends responses to be injected into HTML creator
  });
});